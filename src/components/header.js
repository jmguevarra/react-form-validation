import React from 'react';

function mainHeader(){
    return(
        <header className="main-header">
            <div className="container">
                <h1>components</h1>
            </div>
        </header>
    );
}

export default mainHeader;